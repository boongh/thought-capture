"""Immutable domain types, policies, and ports for Thought Capture AI.

This package is stdlib-only by design (docs/DESIGN.md 5.3). It must not import
web frameworks, database drivers, HTTP clients, or provider SDKs. Ports declared
here are implemented by ``tc_infrastructure``.
"""

__all__: list[str] = []
