# Virtual workspace root: intentionally no [project] table, so the root is never
# importable or publishable. Members live in apps/* and packages/*.
# See docs/DESIGN.md 17 for the boundary each member owns.

[dependency-groups]
dev = [
    "ruff>=0.16.5",
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.0",
    "hypothesis>=6.167",
]

[tool.uv.workspace]
members = ["apps/*", "packages/*"]

[tool.uv.sources]
tc-domain = { workspace = true }
tc-application = { workspace = true }
tc-infrastructure = { workspace = true }
tc-api = { workspace = true }
tc-discord-bot = { workspace = true }
tc-worker = { workspace = true }

# ---------------------------------------------------------------------------
# Lint and format
# ---------------------------------------------------------------------------

[tool.ruff]
line-length = 100
target-version = "py314"
# Never let a formatter rewrite prose. docs/DESIGN.md is the accepted design
# anchor and contains illustrative Python blocks that ruff would otherwise
# reformat; changing it requires an ADR, not a tool run (CLAUDE.md).
extend-exclude = ["docs", "rendered", "*.md"]
src = [
    "packages/domain/src",
    "packages/application/src",
    "packages/infrastructure/src",
    "apps/api/src",
    "apps/discord_bot/src",
    "apps/worker/src",
    "tests",
]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "UP",   # pyupgrade
    "B",    # bugbear
    "A",    # builtins shadowing
    "C4",   # comprehensions
    "DTZ",  # timezone-aware datetimes
    "T20",  # no print
    "SIM",  # simplify
    "PTH",  # use pathlib
    "RUF",
]
ignore = [
    "E501",  # line length is handled by the formatter
]

[tool.ruff.lint.isort]
known-first-party = [
    "tc_domain",
    "tc_application",
    "tc_infrastructure",
    "tc_api",
    "tc_discord_bot",
    "tc_worker",
    "tests",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["T20"]
"tools/**" = ["T20"]
# Domain errors are named for the rule they describe ("CaptureRejected"), which
# reads better at the call site than an Error suffix would.
"packages/domain/src/tc_domain/errors.py" = ["N818"]
# Hypothesis requires *naive* datetimes as the bounds of `st.datetimes` when a
# `timezones` strategy is supplied; DTZ001 cannot tell the two uses apart.
"tests/unit/test_local_calendar_fields.py" = ["DTZ001"]
"tests/unit/test_capture_windows.py" = ["DTZ001"]

# ---------------------------------------------------------------------------
# Types
# ---------------------------------------------------------------------------

[tool.mypy]
python_version = "3.14"
strict = true
warn_unreachable = true
mypy_path = [
    "packages/domain/src",
    "packages/application/src",
    "packages/infrastructure/src",
    "apps/api/src",
    "apps/discord_bot/src",
    "apps/worker/src",
]
files = [
    "packages/domain/src",
    "packages/application/src",
    "packages/infrastructure/src",
    "apps/api/src",
    "apps/discord_bot/src",
    "apps/worker/src",
]

# Strict mode is enforced on domain and application, which carry the business
# rules. Infrastructure and app wiring interact with third-party libraries whose
# stubs are uneven; they are type-checked but may permit untyped imports.
[[tool.mypy.overrides]]
module = ["tc_infrastructure.*", "tc_api.*", "tc_discord_bot.*", "tc_worker.*"]
disallow_untyped_calls = false
disallow_subclassing_any = false

# APScheduler 3.x ships neither inline types nor a py.typed marker, and there is
# no maintained stub package. Scoped to the library itself so first-party code
# stays strictly checked.
[[tool.mypy.overrides]]
module = ["apscheduler.*"]
ignore_missing_imports = true

# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------

[tool.pytest.ini_options]
testpaths = ["tests"]
# Put the repository root on sys.path so shared test helpers import as
# `tests.unit.fakes` rather than by fragile relative path juggling.
pythonpath = ["."]
asyncio_mode = "auto"
markers = [
    "integration: requires a live PostgreSQL instance (docker compose profile 'core')",
    "contract: requires a pinned external service",
]
addopts = "-ra --strict-markers --strict-config"
filterwarnings = ["error"]
